# Sessions

Refresh tokens let a signed-in customer renew a session.
The dashboard and catalog also use the word token for unrelated counters.
Payment events arrive as webhooks. Their integrity is checked before parsing.
